# JS files can be transpiled from .ts files
# No need to commit them
web/**/*.js

# node_modules directory should not be checked in
# this will be generated by running npm install
node_modules/

# Files specific to visual studio code. They don't have any relevance if you are working
# with a different text editor.
.vscode/